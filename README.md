
  # finka azul

  This is a code bundle for finka azul. The original project is available at https://www.figma.com/design/aC5VVH9x8HTDwGNOdi4zSp/finka-azul.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  